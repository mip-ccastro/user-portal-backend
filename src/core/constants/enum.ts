export enum user_role {
    'admin' = 'admin',
    'user' = 'user'
}

export enum user_status {
    'Deleted' = -1,
    'Inactive' = 0,
    'Active' = 1,
    'Suspended' = 2
}

export enum template_type {
    'email' = 'email',
    'sms' = 'sms'
}